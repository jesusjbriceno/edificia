---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { ForgotPassword as ForgotPasswordForm } from '@/components/auth/ForgotPassword';
---

<BaseLayout title="Recuperar Contraseña">
	<main class="min-h-screen flex items-center justify-center p-6 bg-dark-bg relative overflow-hidden">
		<!-- Background Grid & Glow -->
		<div class="absolute inset-0 z-0 overflow-hidden pointer-events-none">
			<div class="absolute -top-[25%] -left-[25%] w-[100%] h-[100%] bg-brand-primary/10 blur-[120px] rounded-full"></div>
			<div class="absolute -bottom-[25%] -right-[25%] w-[100%] h-[100%] bg-brand-secondary/5 blur-[120px] rounded-full"></div>
		</div>

		<div class="w-full max-w-md bg-dark-card/30 backdrop-blur-2xl border border-white/5 p-10 rounded-3xl relative z-10">
			<div class="flex justify-center mb-10">
				<div class="text-3xl font-black text-brand-primary tracking-tighter">
					EDIFICIA<span class="text-white">.</span>
				</div>
			</div>

			<ForgotPasswordForm client:load onSubmit={(email) => console.log('Recovery requested for:', email)} />
		</div>
	</main>
</BaseLayout>
