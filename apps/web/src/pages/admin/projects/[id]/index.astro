---
import DashboardLayout from '@/layouts/DashboardLayout.astro';
import { AuthGuard } from '@/components/auth/AuthGuard';
import AdminProjectDetailPage from '@/components/Admin/AdminProjectDetailPage';

const { id } = Astro.params;
---

<DashboardLayout title="Admin — Detalle de Proyecto">
	<AuthGuard allowedRoles={['Root', 'Admin']} client:load>
		<div class="max-w-7xl mx-auto space-y-8">
			<AdminProjectDetailPage projectId={id!} client:load />
		</div>
	</AuthGuard>
</DashboardLayout>
