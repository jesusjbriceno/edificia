# ─── EdificIA Web Environment Variables ──────────────────────────────
# Copy this file as .env and fill in the values.

# API base URL (local dev — without trailing slash)
PUBLIC_API_URL=http://localhost:5000/api

# Internal API URL (used for SSR login proxy inside Docker network)
# In production (Coolify) this points to the API container: http://api:8080
INTERNAL_API_URL=http://localhost:5000

# Server host/port (optional — defaults to 0.0.0.0:4321)
HOST=0.0.0.0
PORT=4321
